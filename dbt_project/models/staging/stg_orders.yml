version: 2

models:
  - name: stg_orders
    description: "Cleaned and deduplicated synthetic order data."
    columns:
      - name: order_id
        tests:
          - not_null
          - unique
      - name: gross_revenue
        tests:
          - not_null
